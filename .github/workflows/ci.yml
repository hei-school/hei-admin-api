name: CI

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'
  workflow_dispatch:

  workflow_call:

env:
  SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }} 

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    env:
      AWS_REGION: eu-west-3
      VOLA_API_KEY: ${{
        (github.event_name == 'pull_request' && github.base_ref == 'prod') && secrets.PROD_VOLA_API_KEY ||
        (github.event_name == 'pull_request' && github.base_ref == 'preprod') && secrets.PREPROD_VOLA_API_KEY ||
        (github.ref == 'refs/heads/prod') && secrets.PROD_VOLA_API_KEY ||
        (github.ref == 'refs/heads/preprod') && secrets.PREPROD_VOLA_API_KEY
        }}
      VOLA_API_URL: ${{
        (github.event_name == 'pull_request' && github.base_ref == 'prod') && secrets.PROD_VOLA_API_URL ||
        (github.event_name == 'pull_request' && github.base_ref == 'preprod') && secrets.PREPROD_VOLA_API_URL ||
        (github.ref == 'refs/heads/prod') && secrets.PROD_VOLA_API_URL ||
        (github.ref == 'refs/heads/preprod') && secrets.PREPROD_VOLA_API_URL
        }}


    steps:
      - uses: actions/checkout@v4.1.6

      - uses: actions/setup-java@v4.2.1
        with:
          java-version: '21'
          distribution: 'corretto'
      - run: chmod +x gradlew
      - run: chmod +x .shell/publish_gen_to_maven_local.sh
      - run: |
          ./gradlew test \
            -Dvola.api.key="$VOLA_API_KEY" \
            -Dvola.api.url="$VOLA_API_URL"
  
  

  format:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4.1.6
      - uses: actions/setup-java@v4.2.1
        with:
          java-version: '21'
          distribution: 'corretto'

      - run: chmod +x format.sh
      - run: ./format.sh && git diff --exit-code