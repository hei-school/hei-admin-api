We use [OpenAPI Mocker](https://github.com/jormaechea/open-api-mocker) for local mocking:
```
$ npm i -g open-api-mocker
$ open-api-mocker -s ../api.yml -w
```

To customize the local mocker, modify then execute `./mocker.js`.
